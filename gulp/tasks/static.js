var gulp = require('gulp'),
    config = require('../config');

// gulp.task('static', ['libs', 'fonts', 'audio']);
//
// gulp.task('libs', function () {
//     return gulp.src(config.directories.app + '/js/libs/static/*.js')
//         .pipe(plumber())
//         .pipe(chmod(config.chmod))
//         .pipe(gulp.dest(config.directories.public + '/libs'));
// });
